# Open work

**Started 2026-08-01.** Until now the ordered work lived in `PLAN.md` (research), `DATASETS.md`
(what a new source must earn) and the stop conditions inside each method note — all good documents,
none of them a list you can point at. Two items also arrived from a session handoff numbered #29 and
#30, and those numbers existed nowhere in the repository. This file is where numbered work lives
now; the three documents above keep their jobs and are linked from the entries that depend on them.

Numbers are permanent. A finished item keeps its number and moves to the bottom.

## Now

The plan reordered by the owner on 2026-08-08, recorded in
[ADR 0012](adr/0012-the-book-not-the-skeleton.md): the movement arc left a great skeleton, not a
presentation, so the backend finishes first — the green wave (#40), the elk finding (#43),
step-selection (#44), Forecast A (#13), a migratory source if one survives the survey (#49) — and
then one presentation arc (#52) rebuilds the frontend around the story, for version 1.0. Frontend
work before then is triage only (#53).

The modeling direction set by the owner on 2026-08-18, as the descriptive half closed: skill
before forecasts (#54), the coupling graph as the candidate centerpiece (#55), the marine work
redone at the scale its own null demanded (#56), and a standing public prediction the site grades
itself on (#57). Not for a paper — for what the information does. The mission sentence to hold
the arc against: not only where animals move, but what is changing it, and what moves together.

## Queued, with a reason to wait

| # | Item | Why it waits |
| --- | --- | --- |
| 52 | The presentation arc: version 1.0 | [ADR 0012](adr/0012-the-book-not-the-skeleton.md): the book with side tabs, an introduction before any claim, chapters ordered as the argument, and the world as the map in the back pocket. Waits, deliberately, for the backend to finish — a rebuild that presented half the data would be rebuilt again. Opens with its own build-time ADR. Owner input recorded 2026-08-18 for that ADR: consider a flat scrollable world map in the style of an old hand-drawn chart as the primary canvas instead of the globe — lighter, the whole system visible at once, marginalia where the honesty lives (coverage gaps as the map's own here-be-dragons), with the projection chosen kindly to the poles, where Svalbard and the ice edge live. |
| 53 | Explore panel triage | Two live defects reported 2026-08-08: the panel's controls overlap at some widths, and the Play button does not visibly run the year. Both live in the panel #52 dissolves, but the site is live now. Small, and worth a session's first hour rather than waiting for v1.0. |
| 49 | Register a genuinely migratory tracked source | ADR 0010 §6: the lake's tracks follow resident herds because they were chosen for the ledger, and the durable fix is admission, not engineering. A candidate — open licence, low sensitivity, a long-distance migrant, enough animals to clear the k-floor — enters through `DATASETS.md` in the map-layer role `megamove` already holds. Waits on a candidate survey. |
| 50 | Ask a data owner for finer-than-policy permission | Narrowed by ADR 0011: the `low` sources no longer need it. The `OwnerPermission` path — built, tested, never supplied — now matters only for identified lines from a `moderate` source, in practice an elk path. Waits on #38 shipping what needs no asking first. |
| 44 | Phase 2b: step-selection on the cleared terrestrial tracks | Fills `models/ssf/` and answers the README's own unstarted question; its outputs — utilization surfaces, corridors — are globe-native and feed the arc. Pre-register before anything is fitted. |
| 45 | The dual-counterfactual write-up | DAMIP and ATTRICI are already computed and drawn against each other on the site, and `literature-2026-07.md` found no precedent for the application to migration timing. Waits on reading the paywalled *Nature Reviews Biodiversity* review before any novelty claim is made in public — its own stated condition. |
| 46 | Temporal detectability as a named data product | The axis is unnamed in the shortfall literature (`literature-2026-07.md` §4) and the map is built. What remains is packaging: a documented per-cell product with the 4% number and its method. |
| 47 | A fourth leg for the transfer test | `transfer-fails`' failing leg is phenological, radar and unit-converted at once. A non-radar timing record — constant-effort ringing, or a phenology network — separates "timing responds differently" from "the radar is different". Pre-register before fetch, and apply §6's lesson: ask what result the confound list cannot explain. |
| 48 | A visual-ROBITT methods note | The per-claim interactive bias assessment appears unbuilt anywhere else (`literature-2026-07.md` §3). Extracting and documenting the pattern is writing, not engineering; it queues behind the write-up that carries the novelty. |
| 13 | Forecast A: passage date under ScenarioMIP | `DATASETS.md` step 1. One new experiment list in `drivers/cmip6.py`; the novelty mask is the headline. Constrained by #54, 2026-08-18: a forecast is licensed only where hindcast skill exists, which is autumn timing at a scatter of stations -- Forecast A must scope to that or argue a longer-horizon question the era-split does not answer. |
| 55 | The coupling graph: what moves together, and what drives it | **Pre-registered** 2026-08-19 in `docs/methods/phase3c-coupling.md`, reshaped twice before a single pairing was looked at: by its literature pass (no discovered graph -- four registered edges, two of them calibration), and by Phase 3a's prediction 5 (the modes are conditioned on in every edge, because their share of apparent skill is now measured, not feared). Novelty is not claimed anywhere -- the owner has no access to the Suzuki 2026 review, so the stop condition resolved by removing the claim rather than waiting on the read. The CPR landed as the 29th source (90,038 samples, 1958-2022) and **the edges ran once, 2026-08-19**: calibration C2 (SST to plankton bloom) detected cleanly, calibration C1 (winter NAO to band green-up) did not, so the registered stop condition fired -- prediction 1 false, no hypothesis edge interpreted, every interval printed for the record and all of them null. Prediction 3 (the expected plankton-to-fish null) graded true. The published page is the one the registration promised: at these lengths the instrument saw one known signal of two, and claims nothing it cannot see. Any successor registration starts from C1's failure as a measured fact about band-mean green-up. |
| 56 | Marine at its real scale | **Pre-registered** 2026-08-19 in `docs/methods/phase3b-marine-scale.md`, built on a measurement Phase 3a paid for: the whole-record gear rule kept only 7 units, but 17 surveys hold a single-gear segment of twenty or more years -- NEUS-Fall alone recovers a 46-year homogeneous run from a record the binary rule discarded for two refits. The unit is the survey's longest single-gear segment; the estimands are its latitude trend, its own in-situ warming, and a registered cross-unit regression with a depth interaction. Climate velocity is deliberately deferred to its own note -- in-situ beats gridded on the shelf and needs no new ingest. Four fragmented surveys (BITS-1/4, WCTRI, Nor-BTS) were named unsalvageable in advance. **Run 2026-08-19: the stop condition fired** -- the gear salvage delivered its seventeen segments, but only ten carried twenty years of in-situ temperature (the European surveys measure fish without recording the water), below the registered floor of twelve. The phase published as a coverage statement; predictions 1-3 ungradeable by design, prediction 4 false -- it bound to the wrong bottleneck. The reserved extension (OISST over each footprint, at the named cost of a reanalysis instead of the water the fish were in) now has a measured price of seven units and awaits its own registration. |
| 57 | The annual public prediction | Pre-register a falsifiable prediction of the coming season's movement — passage anomaly, green-up-driven herd timing — from observed drivers plus external forecast models, then grade it in public when the year's data lands, hits and misses alike. The two-stage architecture (our fitted responses, other people's forecasts) at its smallest honest scale, and the pre-registration convention turned into the product. The driver is settled: SEAS5 monthly means on the existing CDS token, and its release clock sets the deadline — ECMWF publishes on the 6th of each month at 12 UTC, so each year's prediction must be registered before the 6th of its issue month. #54 has now spoken: the licence extends to autumn radar timing at the stations above chance and no further -- spring is at the false-positive rate, the marine map never formed, the herds fell below their own floor. The prediction, if registered, is an autumn prediction. **Gated further by the owner's direction of 2026-08-19**: before any live prediction, the full two-stage pipeline is graded retrospectively -- `docs/methods/phase3d-rehearsal.md` stands at June 1st of 2017-2024, predicts each autumn with the SEAS5 forecast actually issued then, and collects eight grades today. #57 goes live only if the rehearsal beats its chance bar; the registered expectation is that it will not. |
| 15 | Inline glossary, and a guided path through the five findings | Both strong for a non-technical reader; both explicitly out of scope for the current arc. Pick up on request. |
| 16 | LUH2 route verification | Only if #11 finds a factor effect worth projecting. A projectable driver with no established effect is a solution without a problem. |

## Refused, and why

Kept here so they are not rediscovered as good ideas.

| Item | Reason |
| --- | --- |
| FluxRGNN-style nocturnal-flux nowcast | 220 GiB of vertical profiles, GPU training, and direct competition with BirdCast on the same radar network. `docs/methods/literature-2026-07.md` §2. Revisit only as an explicitly-labelled engineering exercise, never as the novelty claim. |
| ENRAM as a second radar network | One radar of ~190 has fifteen usable autumns, so it cannot carry a trend. Out, not deferred. |
| eBird Status & Trends on the globe | The licence forbids redistribution and the gate enforces it. Analysis only. |
| GRIP4 roads, offshore infrastructure, night lights | `DATASETS.md` gives each its own reason. |
| More attempts at the 2012 latitude-graded step | Four candidate explanations tested, four failed. It stays exposed as an `open` bias domain, which is the honest treatment. Worth one more look only if another source turns up a general instrument-change signature. |

## Done

| # | Item | Outcome |
| --- | --- | --- |
| 41 | Claims ↔ species ↔ layers cross-links | Both directions. Every shift and occupancy study card names and links the claim its evidence feeds, by the claim's own hash address. And the marine claim now carries a specimen — computed, not typed: `_two_way_specimen` scores each species by the smaller of its strongest northward and southward shifts, thresholded at the same FLAT_DEGREES the species cards use, and the lake chose *Raja montagui*, north in one survey and south in another. Findings schema 4; the button threads Shell → Explore → Search as a `preselect` consumed through the exact code path a visitor's own click takes. |
| 42 | Arrival's third door | "Watch a year of movement" joins the two ways out ADR 0007 offered, now that something moves behind the card: it enters explore with the clock already running. Building it flushed out a lie-in-waiting — the panel's Play control initialised to false rather than from the clock, and would have read Play while the year ran. |
| 38 | The herd surfaces and the fox journeys | Three layers from the tracks, under ADR 0010's floors as ADR 0011 redrew them. The Ya Ha Tinda and Svalbard herds as weekly presence surfaces — 481 and 791 cells at 0.01°, k ≥ 3 animals behind every cell-week, both clearing the visibility bar against the lake — and 217 Arctic fox journeys as identified lines: daily medians a decimal coarser than policy so no den resolves to a point, split across silences longer than a week, simplified at one cell of tolerance. The ETHICS ledger gained its first three rows. Building it surfaced two page-level faults, and one of the two was misdiagnosed and corrected a day later. The real fault: the open-by-default attribution had grown into a 321-pixel wall across the sphere's middle — folded to its chip, width-capped when opened. The misdiagnosis: "the tiler drops compact layers below z3.4" was concluded from `queryRenderedFeatures` and answered with locator dots and zoom gates — until the ice layer's pixel probe showed everything drawn at every zoom and only the *query* reporting nothing at globe zooms on this projection. The locators and zoom gates came out; the per-layer zoom hints stayed, re-explained as where a camera must stand for the query instrument to agree with the canvas. An instrument that measures a problem and is never checked against pixels is the `Located.error_km` lesson again, in the browser this time. The payload budget was raised from measurement with its two caveats recorded in place. **Correction, 2026-08-18:** the cell counts above were measured through a bug. The cell label rounded a 0.01° centre to two decimals, which collides adjacent cells — 15.075 and 15.085 both print 15.08 — so the exporter merged their series under one name and published whichever coordinate won a thread race, moving cells between rebuilds. The true surfaces are 1394 and 860 cells, not 791 and 481; the site under-drew both herds by roughly forty percent for ten days. The label now carries one more decimal than its grid, and the exporter refuses any site id that names two places rather than racing for one. |
| 39 | The radar layer given its measured direction | The per-night movement vector — reflectivity-weighted direction and ground speed, in the lake since the first ingest — reduces to a weekly flow climatology per station: magnitude-weighted vector means, medianed across years by component, so 350° and 10° average to north and the heavy nights decide. Published as `dw`/`sw` beside the `w` values (145 → 318 KB) and drawn as a dart per station that turns with the clock and the globe, follows its layer's checkbox and the surface's ink, and is filtered rather than drawn at zero where a week has passage but no velocity fit. The popup gained a heading row. Sanity, read off the rebuilt layer: South Dakota points 325° at 12.8 m/s in mid-May and 159° at 16.3 m/s in early October. |
| 40 | Drivers on the shared clock: the green wave | Both halves shipped. The sea-ice edge went first, 2026-08-08, as 24 monthly median contours on the clock. The green wave followed 2026-08-18: PKU GIMMS NDVI4g (CC BY 4.0, Zenodo, 2.4 GB in four consolidated archives, each MD5-checked and reduced to an npz checkpoint so a broken download resumes rather than restarts) collapsed to a 1982–2022 half-monthly climatology on a 1° grid — 14,308 vegetated cells × 24 bins, 1.24 MB as a seasonal-grid JSON, drawn as a breathing surface that follows the same clock as every herd and dart, with the caption honest that it is a climatology and not any particular year. Registered as the 26th source; the frontend gained the `seasonal` layer kind and a growth ramp in both flavors. The build's one incident: two concurrent build attempts interleaved writes into the same partial download and corrupted it — the checksum caught it, and the build script now takes a lock. |
| 43 | Publish the elk methodological finding (`phase1h`) | `displacement-flat`, the ledger's eighth entry and its second null, judged a probable wrong refusal in the 2026-08-07 handoff and published 2026-08-18. The claim is the method's: winter–summer displacement shows no detectable trend in either herd (−1.84 ± 3.89 km per decade over 341 elk animal-years, +1.16 ± 2.13 over 219 reindeer animal-years) while path length tracks the fix interval at rho −0.879 and displacement does not. Published conditionally, the composition-stable shape: if the escape correlation ever exceeds its registered bar in either herd, the build withholds the finding rather than soften the sentence. The wide null is said loudly in the caveat — anything short of a doubling per decade was invisible — and the phenological domain stays `open`, because fixed calendar windows cannot see a herd that shifted *when* rather than how far. |
| 14 | Tighten the multi-realm ledger test to multi-class | Unblocked the day #43 published. `test_the_terrestrial_realm_is_not_only_birds` replaces the TODO: the ledger's terrestrial claims must span more than one evidence type, which is the class split as the ledger can see it — the atlas claims are SURVEY_INDEX birds, the displacement claim is TRACK mammals, and losing either collapses the land back to one class of animal. |
| 54 | The predictability atlas | Registered, run and published in one day, 2026-08-18. The whole arc rode the existing credentials: the modes became the 27th source, ERA5-Land snow the 28th, the PKU archives gave back their 41 years as 256,119 green-up cell-years, and the harness held the registered words -- era-split ridge, training-era-only statistics, a thousand-fold year-shuffle null per unit. Five predictions: three graded false, one ungradeable by the registration's own arithmetic (the herd requirements intersect at 9 and 10 years against a 15-year floor), and the one survivor a warning -- where the modes alone predict, local weather's marginal contribution halves. The ledger gained `skill-sparse` (direction: limit): autumn timing above chance at 20 of 143 stations, spring at the false-positive rate, marine a coverage statement behind a fired stop condition. Two corrections recorded in the note: the January ERA5 gap that emptied spring's first run, and the first run's dishonest grade over that emptiness. Drawing the map on the globe waits for #52, where an atlas of honest emptiness deserves better presentation than a checkbox layer. |
| 51 | Align the gate with the standard it cites | [ADR 0011](adr/0011-the-gate-aligns-with-its-standard.md), from the owner's question of 2026-08-07. The GBIF best-practices document was read rather than remembered, and the individual table was two categories stricter than it across the board. Three cells moved to the standard's resolutions — not-sensitive as published, `low` to 0.001°, `moderate` to 0.01° — and the house additions that protect something stayed: `high` withheld outright, delays where sensitivity is real, identifiers dropped at `moderate`. Wolves, caribou and every published artifact are byte-identical; the test that pinned the founding rule was rewritten to pin its reversal rather than deleted. |
| 37 | Wire the unreachable entry points | `migratlas ingest sbs`, `ingest jrc-gsw`, `report phase1g` and `report phase1h`, each with a make target, all verified against what the original runs recorded. The JRC command re-fetches its ten tiles — they had been deleted after the run — and its write moved inside the adapter, because the admission guard rightly refused a CLI that wrote to the lake directly. Making the runs repeatable surfaced three unrecorded conventions and one inconsistency: the quadrant split (the footprint's median cell) and the placebo boundary (`np.quantile` linear, 124 cells) are in code now; two seeded spectral p-values moved 0.02–0.03 with no verdict change, recorded in the note as unresolved; and phase1h had quoted a full-width percentage beside a half-width one, recorded as a correction. The ingests reproduce their counts to the row: 992 water samples, 959,802 Swedish ones. |
| 36 | The README brought back to true, and guarded | The status line said five findings and twenty sources against seven and 24 — the same line's second drift. The counts are digits now, and `tests/test_readme_status.py` refuses a build where they disagree with `findings.json` and the registry. The southern-share paragraph, which once said 0.0% for six days after SABAP made it false, now quotes `coverage-bias`'s own shares and is guarded the same way. 1e's phase row got its answer, 1i got a row, MapLibre v5 became v6, and a tracked `grep.exe.stackdump` left the tree. |
| 35 | Ship the transfer arc | `develop` sat nine commits ahead of the `main` Pages deploys from, with `transfer-fails` invisible. Two things the handoff got wrong, found on the way: the gap was nine commits rather than sixty — PR #3 had already shipped the atlas arc on 2026-08-06 — and the gate was not green on the tip. CI failed where the local gate passed, on the coupling filter's platform-luck zero (see the correction in `phase1i-transfer.md`), which is its own small argument for never trusting a same-machine green. Merged as PR #4 after watching both runs, explicitly rather than with `--auto`, per the standing warning. |
| 12 | The transfer test, and the axis it broke along | [`docs/methods/phase1i-transfer.md`](methods/phase1i-transfer.md), published as `transfer-fails`. Three thermal tracking ratios under one audit: aerial-north −0.706 over 69 radar stations, marine-north −0.025 over 1,426 species-and-survey pairs, terrestrial-south −0.014 over 523 species. **The two spatial records agree across the equator** — 0.011 apart, Holm p 0.099, IQR ratio 0.99, each covering 49.7% and 52.8% of the other against a target of 50% — and the phenological one sits 0.68 from both. So transfer failed along the one axis §6 did not list: displacement versus timing, not hemisphere. Two of four predictions failed, including the one naming which realm would be predicted worst, and both are graded as registered. Two corrections recorded rather than edited away: §6's confound list omitted response type, and §4's criterion is n-dependent — it graded marine a failure on an error of 0.056 while coverage put the same prediction at 49.7%. The aerial stop condition did not fire (0.054 against 0.2), so the two-realm fallback was not taken. Honest ceiling: the two realms that agree both sit at no tracking at all, and an absence of response is cheap to reproduce. |
| 18 | The second register, on the three documents that are not the ledger | The site explained itself plainly right up to the moment a reader clicked into the evidence. `counterfactual.json` gains `plain_disagreement`, `detectability.json` a `plain_reason` per withheld source, `sandbox.json` a `plain_why` per knob — schemas 3, 3 and 2, each rendered above its full text rather than instead of it. The refusal lines are held in the reports layer and not on the gate's rule: the gate decides what may be published, and a prose field on a capability is an invitation to soften a reason until it suits the page. A withheld source with no plain line stops the build. |
| 34 | Shipped, and the three things that had to be fixed to ship | `main` was 44 commits behind and Pages deploys only from it, so nothing since 1 August was visible to anyone. Merging exposed a branch CI had never run on: layers loading in series (`ready` 14,577 → 7,224 ms), two coverage guards that read a lake CI does not have, and a long-task budget measuring the runner rather than the page. The budget is a same-machine ratio now — a claim change against a repaint of the same page — because across four runs the absolutes swung 40% while the ratio held between 1.50 and 1.71. Three suspects were profiled out first: the hatch costs 7 ms, the drawn marks barely register (3,332 of 4,009 ms is `(program)`), and the page turn is worth 13 ms. Two setup holes closed with it: CI never ran on `develop` between pull requests, and `gh pr merge --auto` does not wait when no check is required — it merged onto a red build within the second. |
| 11 | The first factor, and it is not detectable *(handoff #29)* | [`docs/methods/phase1g-water.md`](methods/phase1g-water.md). JRC surface water registered, 185 MB fetched, 992 driver rows landed — the lake's first southern driver. Water change explains none of the per-cell atlas change: partial *r* −0.036, spatial nulls p 0.59 and 0.79, one sd of water moving the response 3.2% of one sd. Dropped, as `DATASETS.md` step 3 pre-committed, and **not** published as a finding: the per-year water that would match the atlas windows is not downloadable, so the instrument is attenuated and the null means "not detectable" rather than "absent" — a site claim would over-state it. The placebo is the keeper: naive p 0.031 in a subset chosen to show nothing, spatial p 0.085, which is §4's argument demonstrated on this project's own data. |
| 33 | The site said nobody had looked south, with 19.7M southern rows in the lake | `coverage-bias` published "0.0% ... lie south of the equator" for six days after SABAP made it false. The share was computed from the lake every build; the *two sources to compute it over* were string literals, and `_southern_share`'s own docstring had predicted exactly this. The real number is 35.9% of 54,962,664 rows. Sources are enumerated now, tracks are counted, and a type that falls through `TIME_AXIS`/`POOLED` stops the build. The finding is stronger for being true: **0 of 6,447,050 driver samples are southern**, so the evidence crossed the equator and the explanatory data did not — which is also why #12 cannot be built yet. |
| 32 | The pages had names and nobody could read them | Two thirds of the 4,099 pages were headed by a scientific name because the resolver took its keys from the drawn layers and a studied taxon need not have one — so no run of `make taxon-names` could ever have named them. 93.6% now carry a common name, up from 66.8%, and *Bison bison* reads American Bison. Found alongside it: `occupancy` was missing from the frontend's `StudyKind`, so 560 pages shipped with a kind label reading `undefined`, invisible to `tsc` because the shard JSON is cast rather than validated. |
| 10 | Species pages, wave 2 | 430 new bird pages and 4,099 in total, each carrying the detection-corrected change beside the uncorrected reporting rate so the ledger's methodological finding is visible on one bird. Three kinds of page, and the two that report no number are the point: a species that flips sign between the two epoch-2 windows, and one recorded so widely that detection pins at its boundary. The ethics test caught the cards on a coordinate pattern and was tightened rather than exempted — an occupancy number lives in [-1, 1] and the atlas footprint does not. |
| 31 | The claim that pointed at an empty map | [`docs/methods/phase1f-atlas-surface.md`](methods/phase1f-atlas-surface.md), pre-registered before anything was computed and with the results appended. 496 cells now draw the change in recorded taxa over southern Africa, gated through both atlases' clearances rather than one. **Two of four predictions failed and are recorded as failures:** the absolute thresholds were specified on a per-taxon scale and applied to a sum over 512 taxa, so ±1.5 taxa asserted \|mean Δψ\| < 0.003 against a published median of −0.0071. They are not re-derived. The stop condition that fired sends the *uncorrected* count to the map and withholds the corrected one, which is what ships. The one that mattered passed with the sign wrong for an artefact: cells that gained cards show smaller changes, not larger. |
| 25 | The globe under the night sky, and a colour-vision test | Every basemap value moved from warm brown into the page's own slate blue, and the Viénot simulation runs in the suite instead of sitting in a comment. It found `--detect-short` 43 from `--detect-unknown` under deuteranopia, and that the separations recorded in `tokens.css` did not reproduce. |
| 9 | SABAP occupancy-detection model, and the atlas finding | The sixth finding, and the first that is southern, terrestrial and not a radar. 512 species over 496 shared cells, median Δψ −0.007. Every registered prediction graded — two failed — and every stop condition resolved, including the one that fired: the detection correction the whole design was built for changed the answer by 0.002, because 20 cards per cell leaves nothing for detection to explain. |
| 30 | Sweden registered: the lake's first European time series | Two CC0 sampling-event schemes from Lund, 959,802 rows over 1,972 routes. The summer point counts run 1975-2024, fifty distinct years, the longest series in the lake by twenty. 143 new detectable cells on the coverage map. |
| 29 | The floor refuses a row it was told nothing about | 13,966 fixes of eight animals whose species the archive never recorded were in the lake unscreened: `screen_taxa` dropped nulls before the floor saw them, and no per-taxon sensitivity rule can reach a taxon nobody named. The floor refuses an unnamed row now, and the adapter drops them out loud rather than hiding them from it. |
| 8 | The 617 Missouri bison rows sitting in Berlin | A per-taxon implied-speed ceiling, as filed, plus two things the data showed and the note did not: raw implied speed is unusable across shared timestamps, and the decision has to be per stretch rather than per row. It also found a third artefact — the collars were driven 400 km west on 2022-10-17 — and cost two wrong versions that each deleted real movement. 1,377 rows of 6,047,093 go; MMRV keeps all 1,502. |
| 28 | The detectability layer was drawn while its box read off | Not the diagnosis it was filed with: the initial visibility *was* applied. `exploreView` was handed every layer that loaded and switched them all on, including one declared off, while the panel went on reading the declared value. Explore mode now draws what the layers declare, and a test compares every checkbox against its layer's MapLibre visibility in both directions. |
| 27 | The globe drawn in the same hand | Land hatched in pencil through `fill-pattern`, a hand-ruled graticule, and the coastline drawn twice — each with the bound it needs written as a test, because tiling, closure and positional error are the three things none of this can be eyeballed for. Not the data layers: a circle is a measurement, and wobbling it draws a shape the data does not have. |
| 26 | The paper was not paper | Both textures re-centred on the neutral value of the blend that uses them, and the blend mode added to the two surfaces that painted paper without it and so showed the raw texture. The contrast suite measures the sheet as it is rendered now, not the token underneath it. |
| 24 | The furniture | The scrollbar is a ruled line with a pencil stub on it, the map's zoom and projection buttons are paper chips with drawn marks, and the scale bar is a drawn measure. All generated with the same pen and handed to CSS as data URIs, because none of these can hold an `<svg>`. |
| 23 | Every mark on rough.js | The hand-rolled hash-and-jitter is gone, each mark carries a name the tests can select, and a mark can be regenerated on a palette change rather than restyled. |
| 22 | Type as a setting | Three presets — hand throughout, hand for headings only with Atkinson Hyperlegible under it, and OpenDyslexic — each with its own scale and leading, because the faces do not share an x-height. |
| 21 | The foundation: fonts, textures, tokens | Direction A under the real app. The green split for contrast the way the rust already was, night as a night sky rather than the day page with the lights off, and both the paper texture and the font subsets corrected after both turned out to be measurably not what I had said they were. |
| 1 | Housekeeping | `LICENSE`, `CLAUDE.md`, this file, README status and phase table. |
| 7 | Species pages, wave 1 | 3,669 pages: 755 marine distribution shifts with their per-survey rows, 4 tracked mammals, 2 refusals, and 2,909 that say plainly that nothing here measures this animal. |
| 20 | The sketchbook, finished | The tools panel is on the same torn paper as the claims, the sliders are ruled scales with a pencil-stub thumb, and the last U+2713 on the site is a drawn mark. |
| 6 | The sketchbook rebuild | [ADR 0008](adr/0008-the-sketchbook-rebuild.md). Paper as a torn sheet, every control drawn, the page turn, and a claim with its own address. The architecture stayed; the visual layer was rebuilt. |
| 19 | Night, reachable | A day/night/system switch that survives a reload, black paper in place of the slate blue-grey, a night basemap, and every layer repainting when the surface changes. Six colours that had escaped the token set are in it. |
| 5 | Dual register: plain-language schema v3 | `plain`, `matters` and `plain_caveat` on every `Finding`, required by the schema and by four tests. The plain sentence is the heading, the precise claim is rendered in full underneath behind a "precisely" label, and the site says why a finding is worth knowing for the first time. |
| 4 | Pre-register the SABAP atlas comparison | [`docs/methods/phase1e-atlas.md`](methods/phase1e-atlas.md). The feasibility question it was written to answer came back yes: every row is "recorded on *k* of *n* cards", so detection is identifiable without a card identifier and the stop condition does not fire. |
| 2 | Two published numbers nothing recomputed | The composition claim's airspeed now comes from the fit `phase1c` prints, and is withheld if that fit stops being flat. The coverage limit counted evidence types in use and said five; four are, so it counts them from the lake now. **Reopened and closed properly 2026-08-05:** the code half had been done and the guard half had not, so the rule was being kept by hand. A test now walks `findings.py`'s syntax tree and fails on any `Finding(value=...)` that is a bare string. It was left listed under *Now* for four days after the work landed, as was #1 — which is the drift this file exists to stop, so both rows are gone. |
| 17 | Delete the *Homo sapiens* rows the lake held | 119 rows in `obis_speciesgrids`, ingested before the never-ingested floor existed, removed from nine year partitions with 17,192,885 kept. `migratlas lake-floor` reports and `--apply` deletes; `make lake-floor` is the report. The pooled `marine-taxa-recorded` layer had been counting them as a marine taxon in 44 ocean cells, which the per-taxon gate never saw. |
| 3 | Re-resolve pre-fix taxon keys *(handoff #30)* | **The premise did not hold.** 1,329 taxon keys appear in more than one source and every one agrees, so a join on `taxon_key` was already safe and nothing was blocked. What was wrong was `taxon_label`: 95 keys carry two or more verbatim names across sources, and the search index took whichever it read first. Display names now resolve from the key. Two things were found underneath it — a live human occurrence surface on the site (see below) and `make taxon-index`, which would have replaced the 3,072-taxon search index with a thirty-animal seed list in a shape the frontend cannot parse. Both fixed. |

History before 2026-08-01 is in the git log, whose messages are long-form findings rather than
change summaries, and in the results sections appended to the method notes.
